struct Point {
    x: num,
    y: num
}

struct Circle {
    center: Point,
    radius: num
}

action insertPoint(point: Point) {
    insert point;
}

action deletePoint(point: ref Point) {
    delete point;
}
